<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@color/grey_form" /> <!-- Background color -->

    <stroke
        android:width="2dp"
        android:color="@android:color/darker_gray" /> <!-- Outline color -->

    <corners android:radius="10dp" /> <!-- Adjust the radius to your preference -->

    <padding
        android:left="10dp"
        android:top="10dp"
        android:right="10dp"
        android:bottom="10dp" /> <!-- Padding inside the EditText -->
</shape>
